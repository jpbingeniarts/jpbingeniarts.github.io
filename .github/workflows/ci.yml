name: Build


on:
  push:
    branches:
      - main
      - release/**

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build dev application
        run: echo ceci est un build avec les config de dev

  test:
    needs: build
    runs-on: ubuntu-latest

    steps:

      - name: Test application
        run: echo ceci est un build
